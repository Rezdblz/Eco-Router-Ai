version: "3.9"

services:
  router:
    build: .
    container_name: eco-router

    depends_on:
      - ollama

    environment:
      OLLAMA_URL: http://ollama:11434

  ollama:
    build: ./ollama
    container_name: eco-router-llm

    ports:
      - "11434:11434"